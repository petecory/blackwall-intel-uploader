# Runtime deps for the GUI + system tray. The --cli mode is standard-library only.
pystray>=0.19
pillow>=10.0
# Build a binary with:
#   pip install pyinstaller
#   pyinstaller --onefile --windowed eve_intel_uploader.py   (Windows: no console)
